@using System.ComponentModel.DataAnnotations
@using CleanArchitecture.Blazor.Server.UI.Resources.ValidationMessages
@using CleanArchitecture.Blazor.Server.UI.Services
@using CleanArchitecture.Blazor.Application.Common.Models
@using CleanArchitecture.Blazor.Domain.Common.Enums
@using CleanArchitecture.Blazor.Application.Common.Constants
@using CleanArchitecture.Blazor.Application.Common.ExceptionHandlers
@using CleanArchitecture.Blazor.Application.Common.Interfaces.Identity
@inherits OwningComponentBase
@inject IStringLocalizer<CleanArchitecture.Blazor.Server.UI.Pages.Identity.Users.Profile> L
@inject IFileUploadService UploadService

@inject IUserProfileState UserProfileState
@implements IDisposable

@if (_profileModel != null)
{
    <EditForm Model="@_profileModel" OnValidSubmit="HandleValidSubmitAsync">
        <DataAnnotationsValidator />

        <!-- User Profile Avatar & Header (Matching Screenshot) -->
        <div class="profile-avatar-header">
            <div class="profile-avatar-wrapper">
                @if (string.IsNullOrEmpty(_profileModel.ProfilePictureDataUrl))
                {
                    <MudAvatar Class="profile-avatar" Color="Color.Primary">
                        @(string.IsNullOrEmpty(_profileModel.UserName) ? "" : _profileModel.UserName.ToUpper().First().ToString())
                    </MudAvatar>
                }
                else
                {
                    <MudAvatar Class="profile-avatar">
                        <MudImage Src="@_profileModel.ProfilePictureDataUrl" Alt="@_profileModel.DisplayName" />
                    </MudAvatar>
                }

                <!-- Floating Camera Action Menu Button -->
                <MudMenu AnchorOrigin="Origin.BottomRight" TransformOrigin="Origin.TopLeft" Dense="true">
                    <ActivatorContent Context="menuContext">
                        <button type="button" class="profile-avatar-camera-btn" title="@L["Change avatar"]"
                                aria-label="@L["Change avatar"]" @onclick="menuContext.ToggleAsync">
                            <MudIcon Icon="@Icons.Material.Filled.PhotoCamera" Size="MudBlazor.Size.Small" />
                        </button>
                    </ActivatorContent>
                    <ChildContent>
                        <MudMenuItem Icon="@Icons.Material.Outlined.FileUpload">
                            <MudFileUpload T="IBrowserFile" Accept=".jpg, .jpeg, .png, .webp" FilesChanged="HandleFileChangedAsync" Class="d-block w-100">
                                <CustomContent Context="uploadContext">
                                    <span class="d-inline-block w-100" @onclick="@uploadContext.OpenFilePickerAsync">
                                        @L["Upload photo"]
                                    </span>
                                </CustomContent>
                                <SelectedTemplate Context="selectedContext"></SelectedTemplate>
                            </MudFileUpload>
                        </MudMenuItem>
                        <MudMenuItem Icon="@Icons.Material.Outlined.DeleteOutline"
                                     Disabled="@(string.IsNullOrEmpty(_profileModel.ProfilePictureDataUrl))"
                                     OnClick="HandleRemovePhotoAsync">
                            @L["Remove photo"]
                        </MudMenuItem>
                    </ChildContent>
                </MudMenu>
            </div>

            <div class="profile-header-info">
                <MudText Typo="Typo.h5" Class="profile-header-name">
                    @(string.IsNullOrEmpty(_profileModel.DisplayName) ? _profileModel.UserName : _profileModel.DisplayName)
                </MudText>
                <div class="profile-header-email">@_profileModel.Email</div>
                @if (_profileModel.AssignedRoles is not null)
                {
                    <div class="d-flex align-center flex-wrap gap-1">
                        @foreach (var role in _profileModel.AssignedRoles)
                        {
                            <span class="profile-role-pill">@role</span>
                        }
                    </div>
                }
            </div>
        </div>

        <MudDivider Class="profile-divider" />

        <!-- Personal details Section -->
        <div class="profile-section">
            <div class="profile-section__header">
                <MudText Typo="Typo.subtitle1" Class="profile-section__title">@L["Personal details"]</MudText>
                <MudText Typo="Typo.caption" Class="profile-section__desc">@L["Update your personal and contact details."]</MudText>
            </div>
            <MudGrid Spacing="3">
                <MudItem xs="12" md="6">
                    <MudTextField For="@(() => _profileModel.DisplayName)" @bind-Value="_profileModel.DisplayName"
                                  Label="@L["Full Name"]" Placeholder="@L["Enter your full name"]"
                                  Variant="Variant.Outlined" Margin="Margin.Dense" />
                </MudItem>
                <MudItem xs="12" md="6">
                    <MudTextField For="@(() => _profileModel.PhoneNumber)" @bind-Value="_profileModel.PhoneNumber"
                                  Label="@L["Phone Number"]" Placeholder="@L["Enter your phone number"]"
                                  Variant="Variant.Outlined" Margin="Margin.Dense" />
                </MudItem>
                <MudItem xs="12" md="6">
                    <TimeZoneAutocomplete T="string" For="@(() => _profileModel.TimeZoneId)" @bind-Value="_profileModel.TimeZoneId"
                                          Label="@L["Time Zone"]" Placeholder="@L["Select a time zone"]"
                                          Variant="Variant.Outlined" Margin="Margin.Dense" />
                </MudItem>
                <MudItem xs="12" md="6">
                    <LanguageAutocomplete T="string" For="@(() => _profileModel.LanguageCode)" @bind-Value="_profileModel.LanguageCode"
                                          Label="@L["Language"]" Placeholder="@L["Select a language"]"
                                          Variant="Variant.Outlined" Margin="Margin.Dense" />
                </MudItem>
            </MudGrid>
        </div>

        <!-- Organization & Account Information Section -->
        <div class="profile-section">
            <div class="profile-section__header">
                <MudText Typo="Typo.subtitle1" Class="profile-section__title">@L["Account & Organization"]</MudText>
                <MudText Typo="Typo.caption" Class="profile-section__desc">@L["Information managed by your organization administration."]</MudText>
            </div>
            <MudGrid Spacing="3">
                <MudItem xs="12" md="6">
                    <MudTextField For="@(() => _profileModel.UserName)" @bind-Value="_profileModel.UserName"
                                  Label="@L["User Name"]" ReadOnly="true"
                                  Variant="Variant.Outlined" Margin="Margin.Dense" />
                </MudItem>
                <MudItem xs="12" md="6">
                    <MudTextField For="@(() => _profileModel.Email)" @bind-Value="_profileModel.Email"
                                  Label="@L["Email"]" ReadOnly="true"
                                  Variant="Variant.Outlined" Margin="Margin.Dense" />
                </MudItem>
                <MudItem xs="12" md="6">
                    <MudTextField For="@(() => _profileModel.TenantName)" @bind-Value="_profileModel.TenantName"
                                  Label="@L["Tenant Name"]" ReadOnly="true" ShrinkLabel="true"
                                  Variant="Variant.Outlined" Margin="Margin.Dense" />
                </MudItem>
                <MudItem xs="12" md="6">
                    <MudTextField For="@(() => _profileModel.SuperiorName)" @bind-Value="_profileModel.SuperiorName"
                                  Label="@L["Superior"]" ReadOnly="true" ShrinkLabel="true"
                                  Variant="Variant.Outlined" Margin="Margin.Dense" />
                </MudItem>
            </MudGrid>
        </div>

        <!-- Actions -->
        <div class="d-flex justify-end mt-4">
            <MudButton ButtonType="ButtonType.Submit" Variant="Variant.Filled" Color="Color.Primary"
                       StartIcon="@Icons.Material.Outlined.Save">
                @AppStrings.Save
            </MudButton>
        </div>
    </EditForm>
}
 
@code {
    private UserProfile UserProfile => UserProfileState.Value;
    private ChangeUserProfileModel? _profileModel { get; set; } = null!;
    private UserManager<ApplicationUser> _userManager { get; set; } = null!;
    protected override Task OnInitializedAsync()
    {
        _userManager = ScopedServices.GetRequiredService<UserManager<ApplicationUser>>();
        InitializeProfileModel();
        // Subscribe to UserProfileState changes
        UserProfileState.Changed += HandleUserProfileChanged;
        return Task.CompletedTask;
    }

    public void Dispose()
    {
        // Unsubscribe from UserProfileState changes
        UserProfileState.Changed -= HandleUserProfileChanged;
    }

    private void HandleUserProfileChanged(object? sender, UserProfile userProfile)
    {
        // Re-initialize the profile model when the service state changes
        InitializeProfileModel();
        InvokeAsync(StateHasChanged);
    }

    private void InitializeProfileModel()
    {
        if (UserProfile != null)
        {
            _profileModel = new ChangeUserProfileModel
            {
                UserId = UserProfile.UserId,
                UserName = UserProfile.UserName,
                Email = UserProfile.Email,
                Provider = UserProfile.Provider,
                SuperiorName = UserProfile.Superior?.UserName,
                SuperiorId = UserProfile.SuperiorId,
                ProfilePictureDataUrl = UserProfile.ProfilePictureDataUrl,
                DisplayName = UserProfile.DisplayName,
                PhoneNumber = UserProfile.PhoneNumber,
                DefaultRole = UserProfile.DefaultRole,
                AssignedRoles = UserProfile.AssignedRoles,
                IsActive = UserProfile.IsActive,
                TenantId = UserProfile.TenantId,
                TenantName = UserProfile.Tenant?.Name,
                TimeZoneId = UserProfile.TimeZoneId,
                LanguageCode = UserProfile.LanguageCode
            };
        }
        else
        {
            _profileModel = null;
        }
    }
    
    private async Task HandleFileChangedAsync(IBrowserFile file)
    {
        await UploadProfilePhotoAsync(file);
    }

    private async Task HandleRemovePhotoAsync()
    {
        if (_profileModel == null) return;

        _profileModel.ProfilePictureDataUrl = null;
        await UserProfileState.UpdateAsync(profile => profile with
        {
            ProfilePictureDataUrl = null
        });

        Snackbar.Add(L["The avatar has been removed"], Severity.Info);
    }

    private async Task HandleValidSubmitAsync(EditContext context)
    {
        if (_profileModel == null) return;

        await UserProfileState.UpdateAsync(profile => profile with
        {
            ProfilePictureDataUrl = _profileModel.ProfilePictureDataUrl,
            DisplayName = _profileModel.DisplayName,
            PhoneNumber = _profileModel.PhoneNumber,
            TimeZoneId = _profileModel.TimeZoneId,
            LanguageCode = _profileModel.LanguageCode
        });

        Snackbar.Add(AppStrings.SaveSuccess, Severity.Info);
    }

    private async Task UploadProfilePhotoAsync(IBrowserFile file)
    {
        if (_profileModel == null) return;

        using var fileStream = file.OpenReadStream(GlobalVariables.MaxAllowedSize);
        using var memoryStream = new MemoryStream();
        await fileStream.CopyToAsync(memoryStream);
        byte[] fileData = memoryStream.ToArray();

        var resizedStream = await ImageProcessor.ResizeAndCompressToJpegAsync(memoryStream, 128, 128, 80);
        var uploadRequest = new UploadRequest(file.Name, UploadType.ProfilePicture, resizedStream.ToArray(), overwrite: true, _profileModel.UserId);

        var result = await UploadService.UploadAsync(uploadRequest);
        if (result.Succeeded && result.Data is not null)
        {
            _profileModel.ProfilePictureDataUrl = result.Data.Url;

            // Update database and state with new profile picture
            await UserProfileState.UpdateAsync(profile => profile with
            {
                ProfilePictureDataUrl = result.Data.Url
            });

            Snackbar.Add(L["The avatar has been updated"], Severity.Info);
        }
        else
        {
            Snackbar.Add(result.ErrorMessage, Severity.Error);
        }
    }



    public class ChangeUserProfileModel
    {
        public string? Provider { get; set; }

        [Display(Name = "Superior")]
        [MaxLength(100,
            ErrorMessageResourceType = typeof(ValidationMessages),
            ErrorMessageResourceName = nameof(ValidationMessages.MaxLengthExceeded))]
        public string? SuperiorName { get; set; }

        public string? SuperiorId { get; set; }

        public string? ProfilePictureDataUrl { get; set; }

        [Display(Name = "Full Name")]
        [MaxLength(50,
            ErrorMessageResourceType = typeof(ValidationMessages),
            ErrorMessageResourceName = nameof(ValidationMessages.MaxLengthExceeded))]
        public string? DisplayName { get; set; }

        [Required(
            ErrorMessageResourceType = typeof(ValidationMessages),
            ErrorMessageResourceName = nameof(ValidationMessages.UserNameRequired))]
        [Display(Name = "User Name")]
        [MaxLength(30,
            ErrorMessageResourceType = typeof(ValidationMessages),
            ErrorMessageResourceName = nameof(ValidationMessages.MaxLengthExceeded))]
        public required string UserName { get; set; }

        [Required(
            ErrorMessageResourceType = typeof(ValidationMessages),
            ErrorMessageResourceName = nameof(ValidationMessages.EmailRequired))]
        [EmailAddress(
            ErrorMessageResourceType = typeof(ValidationMessages),
            ErrorMessageResourceName = nameof(ValidationMessages.EmailInvalid))]
        [Display(Name = "Email")]
        [MaxLength(50,
            ErrorMessageResourceType = typeof(ValidationMessages),
            ErrorMessageResourceName = nameof(ValidationMessages.MaxLengthExceeded))]
        public required string Email { get; set; }

        [Phone(
            ErrorMessageResourceType = typeof(ValidationMessages),
            ErrorMessageResourceName = nameof(ValidationMessages.PhoneNumberInvalid))]
        [Display(Name = "Phone Number")]
        [MaxLength(20,
            ErrorMessageResourceType = typeof(ValidationMessages),
            ErrorMessageResourceName = nameof(ValidationMessages.MaxLengthExceeded))]
        public string? PhoneNumber { get; set; }

        public string? DefaultRole { get; set; }
        public string[]? AssignedRoles { get; set; }

        [Required]
        public required string UserId { get; set; } = Guid.NewGuid().ToString();

        public bool IsActive { get; set; }
        public string? TenantId { get; set; }
        public string? TenantName { get; set; }
        public string? TimeZoneId { get; set; }
        public string? LanguageCode { get; set; }
    }
}
