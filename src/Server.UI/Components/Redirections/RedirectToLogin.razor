@code {

    protected override async Task OnInitializedAsync()
    {
        await Task.Yield(); 
        var uri = new Uri(Navigation.Uri);
        var returnUrl = uri.PathAndQuery.TrimStart('/');
        Navigation.NavigateTo($"/pages/authentication/login?returnUrl={returnUrl}", true);
    }

}